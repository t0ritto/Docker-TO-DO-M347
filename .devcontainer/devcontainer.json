{
    "name": "Node.js",
    "dockerFile": "Dockerfile",
    "settings": {
        "#terminal.integrated.defaultProfile.osx#": "/bin/bash" // Anpassung für macOS
    },
    "extensions": [
        "dbaeumer.vscode-eslint"
    ]
}
